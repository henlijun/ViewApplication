<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lj.module_huizhi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ViewApplication">
        <activity android:name=".ui.AnimationActivity"></activity>
        <activity android:name=".ui.SpanableStringActivity" />
        <activity android:name=".ui.GroupFlowLayoutActivity" />
        <activity android:name=".ui.WatchActivity" />
        <activity android:name=".ui.LTextViewActivity" />
        <activity android:name=".ui.HuiZhiActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>